# Household CSV Demo

Synthetic household registry source for a clean non-farmer happy path.

It demonstrates that Registry Forge can prepare a second domain profile,
semantic alignment set, Crosswalk mapping, redacted preview, readiness report,
and export package without changing the CLI.
