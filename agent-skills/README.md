# Registry Forge Agent Skills

Registry Forge Codex skills are now maintained in the public `registry-skills`
plugin repository.

Canonical skill:

- `registry-forge-operator`

Use the `registry-skills` plugin instead of editing repo-local copies here.
Forge skills must keep the Forge boundary intact: candidate artifacts are
review inputs, while runtime Relay, Notary, and registryctl config generation
and validation stay in the owning product flows.
